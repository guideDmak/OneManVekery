@model AdminProfileViewModel

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Profile";
    ViewData["AdminSection"] = "Profile";
}

<section class="admin-page-banner">
    <div>
        <p class="admin-page-kicker">Profile</p>
        <h2>@Model.FullName</h2>
        <span>@Model.Role</span>
    </div>
    <span class="admin-pill-tag">Bakery Team</span>
</section>

<section class="admin-detail-grid">
    <article class="admin-card admin-profile-card">
        <div class="admin-profile-badge">MB</div>
        <h3>@Model.FullName</h3>
        <p>@Model.Bio</p>

        <div class="admin-summary-list">
            <div class="admin-summary-row">
                <div>
                    <small>Email</small>
                    <strong>@Model.Email</strong>
                </div>
                <span>Main contact</span>
            </div>
            <div class="admin-summary-row">
                <div>
                    <small>Phone</small>
                    <strong>@Model.Phone</strong>
                </div>
                <span>Direct line</span>
            </div>

            @foreach (var item in Model.SummaryItems)
            {
                <div class="admin-summary-row">
                    <div>
                        <small>@item.Label</small>
                        <strong>@item.Value</strong>
                    </div>
                    <span>@item.Detail</span>
                </div>
            }
        </div>
    </article>

    <article class="admin-card">
        <div class="admin-card-head">
            <h2>Preferences</h2>
        </div>

        <div class="admin-summary-list">
            @foreach (var item in Model.PreferenceItems)
            {
                <div class="admin-summary-row">
                    <div>
                        <small>@item.Label</small>
                        <strong>@item.Value</strong>
                    </div>
                    <span>@item.Detail</span>
                </div>
            }
        </div>
    </article>
</section>
